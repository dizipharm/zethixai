import React, { Component } from "react";

interface ReturnsProps {}

interface ReturnsState {}

class Returns extends React.Component<ReturnsProps, ReturnsState> {
  state = {};
  render() {
    return <div>Returns</div>;
  }
}

export default Returns;
